"""MoveIt2 bridge implementation split by responsibility.

ROS message modules are imported lazily inside the boundary functions so this
package remains importable in unit-test environments without ROS2.
"""
